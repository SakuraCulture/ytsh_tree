# 门店商品表数据库设计报告

## 一、表关系概览

```mermaid
erDiagram
    STORE_TABLE ||--o{ STORE_PRODUCT_TABLE : "门店拥有"
    PRODUCT_SKU_TABLE ||--o{ STORE_PRODUCT_TABLE : "SKU属于"
    STORE_PRODUCT_TABLE ||--o{ STORE_STOCK_TABLE : "商品有"
    WAREHOUSE_TABLE ||--o{ WAREHOUSE_STORE_SUPPLY_TABLE : "仓库供货"
    STORE_PRODUCT_TABLE ||--o{ WAREHOUSE_STORE_SUPPLY_TABLE : "商品被供货"

    STORE_TABLE {
        bigint store_id PK "门店ID"
        varchar store_code "门店编码"
        varchar store_name "门店名称"
        int store_status "门店状态"
    }

    PRODUCT_SKU_TABLE {
        bigint product_sku_id PK "SKU ID"
        bigint product_spu_id FK "所属SPU"
        varchar product_sku_code "SKU编码"
        varchar product_sku_name "SKU名称"
        decimal product_cost_price "基准成本价"
        decimal product_retail_price "基准零售价"
    }

    STORE_PRODUCT_TABLE {
        bigint store_product_id PK "门店商品ID"
        bigint store_id FK "门店ID"
        bigint product_sku_id FK "商品SKU"
        varchar store_product_code "门店编码"
        varchar store_product_ownership "商品归属"
        int store_product_pos_status "POS状态"
        decimal store_product_price "门店零售价"
        int store_product_is_active "是否入店"
        datetime store_product_first_date "首次入店日期"
        datetime store_product_shelf_time "上架时间"
    }

    STORE_STOCK_TABLE {
        bigint store_stock_id PK "门店库存ID"
        bigint store_product_id FK "门店商品"
        decimal store_stock_qty "库存数量"
        decimal store_stock_available_qty "可用量"
        decimal store_stock_transit_qty "在途数量"
        decimal store_stock_frozen_qty "冻结库存"
        int store_stock_outstock_hours "缺货时长"
    }

    WAREHOUSE_STORE_SUPPLY_TABLE {
        bigint supply_id PK "供货关系ID"
        bigint store_product_id FK "门店商品"
        bigint warehouse_id FK "仓库ID"
        int supply_is_primary "是否主供货仓"
        int supply_is_active "是否供货"
        int supply_status "供货状态"
    }
```

---

## 二、核心表结构

### 2.1 门店商品表 (store_product_table)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `store_product_id` | bigint | 门店商品ID（主键） |
| `store_id` | bigint | 门店ID（外键→门店表） |
| `product_sku_id` | bigint | 商品SKU（外键→SKU表） |
| `store_product_code` | varchar | 门店商品编码 |
| `store_product_ownership` | varchar | 商品归属（自营/联营/加盟） |
| `store_product_pos_status` | int | POS状态（0未上架/1已上架） |
| `store_product_price` | decimal | 门店零售价 |
| `store_product_is_active` | int | 是否入店（0否/1是） |
| `store_product_first_date` | datetime | 首次入店日期 |
| `store_product_shelf_time` | datetime | 上架时间 |

### 2.2 门店库存表 (store_stock_table)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `store_stock_id` | bigint | 门店库存ID（主键） |
| `store_product_id` | bigint | 门店商品ID（外键） |
| `store_stock_qty` | decimal | 库存数量 |
| `store_stock_available_qty` | decimal | 可用数量 |
| `store_stock_transit_qty` | decimal | 在途数量 |
| `store_stock_frozen_qty` | decimal | 冻结库存 |
| `store_stock_outstock_hours` | int | 缺货时长（小时） |

### 2.3 仓库门店供货表 (warehouse_store_supply_table)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `supply_id` | bigint | 供货关系ID（主键） |
| `store_product_id` | bigint | 门店商品ID（外键） |
| `warehouse_id` | bigint | 仓库ID（外键→仓库表） |
| `supply_is_primary` | int | 是否主供货仓（0否/1是） |
| `supply_is_active` | int | 是否供货（0否/1是） |
| `supply_status` | int | 供货状态 |

---

## 三、表关联关系图

```mermaid
flowchart LR
    subgraph 商品主数据
        SPU[product_spu_table<br/>SPU表]
        SKU[product_sku_table<br/>SKU表]
        UPC[product_upc_table<br/>UPC表]
    end

    subgraph 门店模块
        STORE[store_table<br/>门店表]
        STORE_PROD[store_product_table<br/>门店商品表]
        STORE_STOCK[store_stock_table<br/>门店库存表]
        STORE_SUPPLY[warehouse_store_supply_table<br/>供货关系表]
    end

    subgraph 仓库模块
        WAREHOUSE[warehouse_table<br/>仓库表]
        WAREHOUSE_PROD[warehouse_product_table<br/>仓库商品表]
        WAREHOUSE_STOCK[warehouse_stock_table<br/>仓库库存表]
    end

    SPU --> SKU
    SKU --> UPC
    STORE --> STORE_PROD
    SKU --> STORE_PROD
    STORE_PROD --> STORE_STOCK
    STORE_PROD --> STORE_SUPPLY
    WAREHOUSE --> STORE_SUPPLY
    WAREHOUSE --> WAREHOUSE_PROD
    SKU --> WAREHOUSE_PROD
    WAREHOUSE_PROD --> WAREHOUSE_STOCK
```

---

## 四、查询示例

### 4.1 查询某门店的所有商品及库存

```sql
SELECT
    s.store_name,
    sp.store_product_code,
    sku.product_sku_name,
    sku.product_sku_code,
    sp.store_product_price,
    ss.store_stock_qty,
    ss.store_stock_available_qty
FROM store_product_table sp
JOIN store_table s ON sp.store_id = s.store_id
JOIN product_sku_table sku ON sp.product_sku_id = sku.product_sku_id
LEFT JOIN store_stock_table ss ON sp.store_product_id = ss.store_product_id
WHERE s.store_id = 1;
```

### 4.2 查询某商品的门店分布

```sql
SELECT
    sku.product_sku_name,
    s.store_name,
    sp.store_product_code,
    sp.store_product_price,
    ss.store_stock_qty,
    w.supply_is_primary
FROM store_product_table sp
JOIN product_sku_table sku ON sp.product_sku_id = sku.product_sku_id
JOIN store_table s ON sp.store_id = s.store_id
LEFT JOIN store_stock_table ss ON sp.store_product_id = ss.store_product_id
LEFT JOIN warehouse_store_supply_table w ON sp.store_product_id = w.store_product_id
WHERE sku.product_sku_id = 1001;
```

---

## 五、业务流程图

```mermaid
sequenceDiagram
    participant SPU as SPU表
    participant SKU as SKU表
    participant Store as 门店
    participant StoreProd as 门店商品
    participant StoreStock as 门店库存
    participant Warehouse as 仓库

    Note over StoreProd: 新增门店商品
    Store->>StoreProd: 1. 创建门店商品档案
    SKU->>StoreProd: 2. 关联SKU
    StoreProd->>StoreStock: 3. 初始化库存

    Note over StoreStock: 库存更新流程
    Warehouse->>StoreStock: 4. 供货入库
    Store->>StoreStock: 5. 销售出库
    Store->>StoreStock: 6. 调拨调整
```

---

## 六、总结

| 维度 | 说明 |
|------|------|
| **核心实体** | 门店商品 = 门店 + SKU + 价格策略 |
| **库存实体** | 门店库存 = 可用 + 在途 + 冻结 |
| **供货关系** | 多仓库对一个门店商品供货，区分主供货仓 |
| **商品流转** | SPU → SKU → 门店商品 → 门店库存 |
